import { Input } from "../ui/input";

export default { title: "UI/Input", component: Input };

export const Default = { args: { placeholder: "Type something..." } };
export const Disabled = { args: { placeholder: "Disabled input", disabled: true } };
